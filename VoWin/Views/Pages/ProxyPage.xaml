<Page
  x:Class="VoWin.Views.Pages.ProxyPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:helpers="clr-namespace:VoWin.Helpers"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:vm="clr-namespace:VoWin.ViewModels.Pages"
  Title="网络分流与代理"
  d:DataContext="{d:DesignInstance vm:ProxyViewModel, IsDesignTimeCreatable=False}"
  d:DesignHeight="720"
  d:DesignWidth="1100"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource AppTextPrimaryBrush}"
  Foreground="{DynamicResource AppTextPrimaryBrush}"
  mc:Ignorable="d"
>
  <Page.Resources>
    <helpers:BoolToVisibilityConverter x:Key="BoolToVis" />
    <helpers:BoolToVisibilityConverter x:Key="InvertBoolToVis" Invert="True" />
    <helpers:NullToVisibilityConverter x:Key="NullToVis" />
  </Page.Resources>

  <Grid Margin="24,18,24,24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Top Header -->
    <Grid Grid.Row="0" Margin="0,0,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0" VerticalAlignment="Center">
        <TextBlock Style="{DynamicResource AppPageTitleStyle}" Text="代理分流" />
        <TextBlock Style="{DynamicResource AppPageSubtitleStyle}" Text="ICCID 卡规则优先；未配置时按 IMSI MCC/PLMN 选择国家出口" />
      </StackPanel>

      <ui:Button
        Grid.Column="1"
        VerticalAlignment="Center"
        Appearance="Secondary"
        Command="{Binding TestAllPresetsCommand}"
        Icon="{ui:SymbolIcon Symbol=Pulse24}"
        Content="批量测速所有节点"
      />
    </Grid>

    <!-- Status Banner -->
    <Border
      Grid.Row="1"
      Margin="0,0,0,12"
      Padding="14,8"
      Background="{DynamicResource AppCardBrush}"
      BorderBrush="{DynamicResource AppCardBorderBrush}"
      BorderThickness="1"
      CornerRadius="8"
    >
      <Border.Effect>
        <DropShadowEffect BlurRadius="8" Direction="270" Opacity="0.04" ShadowDepth="1" Color="{DynamicResource AppShadowColor}" />
      </Border.Effect>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ui:SymbolIcon Grid.Column="0" Symbol="Info24" FontSize="15" Margin="0,0,10,0" Foreground="{DynamicResource AppAccentBrush}" Visibility="{Binding IsTesting, Converter={StaticResource InvertBoolToVis}}" />
        <ui:ProgressRing Grid.Column="1" Width="15" Height="15" Margin="0,0,10,0" IsIndeterminate="True" Visibility="{Binding IsTesting, Converter={StaticResource BoolToVis}}" />
        <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding StatusMessage, Mode=OneWay}" />
      </Grid>
    </Border>

    <!-- Main Content TabControl -->
    <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">

      <!-- TAB 1: Per-card ICCID rules (highest priority) -->
      <TabItem Header="SIM 卡规则（ICCID 优先）">
        <Grid Margin="0,12,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>

          <Border Grid.Row="0" Margin="0,0,0,12" Padding="16,14" Background="{DynamicResource AppCardBrush}" BorderBrush="{DynamicResource AppCardBorderBrush}" BorderThickness="1" CornerRadius="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                  <ui:SymbolIcon Symbol="Sim24" FontSize="18" Foreground="{DynamicResource AppAccentBrush}" Margin="0,0,8,0" />
                  <TextBlock FontSize="14" FontWeight="SemiBold" Text="为当前 SIM 设置最高优先级出口" />
                </StackPanel>
                <TextBlock Grid.Column="1" FontSize="11" Foreground="{DynamicResource AppTextTertiaryBrush}" Text="ICCID ＞ IMSI MCC/PLMN ＞ 直连" />
              </Grid>

              <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="2*" MinWidth="220" />
                  <ColumnDefinition Width="2*" MinWidth="220" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ComboBox
                  Grid.Column="0"
                  Margin="0,0,10,0"
                  ItemsSource="{Binding IccidCardOptions}"
                  SelectedItem="{Binding SelectedIccidCard, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Padding="10,5"
                  MaxDropDownHeight="360"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                >
                  <ComboBox.ItemTemplate>
                    <DataTemplate>
                      <StackPanel Margin="0,2">
                        <TextBlock FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="{Binding CardAndNumberDisplay, Mode=OneWay}" />
                        <TextBlock FontSize="10" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource AppTextTertiaryBrush}" Margin="0,2,0,0" Text="{Binding IdentityDisplay, Mode=OneWay}" />
                        <TextBlock FontSize="10" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,1,0,0" Text="{Binding NetworkDisplay, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
                      </StackPanel>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
                <ComboBox Grid.Column="1" Margin="0,0,10,0" ItemsSource="{Binding AvailableEgressOptions}" SelectedItem="{Binding SelectedIccidEgress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="DisplayName" Padding="10,7" />
                <ui:Button Grid.Column="2" Appearance="Primary" Padding="18,8" Command="{Binding SaveIccidRuleCommand}" Icon="{ui:SymbolIcon Symbol=Save24}" Content="保存 / 更新规则" />
              </Grid>

              <StackPanel Grid.Row="2" Margin="2,8,0,0">
                <TextBlock FontSize="11" Foreground="{DynamicResource AppAccentBrush}" Text="{Binding SelectedIccidText, Mode=OneWay}" />
                <TextBlock FontSize="10" Foreground="{DynamicResource AppTextTertiaryBrush}" Margin="0,2,0,0" Text="{Binding SelectedIccidImsiText, Mode=OneWay}" />
              </StackPanel>
            </Grid>
          </Border>

          <Border Grid.Row="1" Padding="16" Background="{DynamicResource AppCardBrush}" BorderBrush="{DynamicResource AppCardBorderBrush}" BorderThickness="1" CornerRadius="12">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontSize="14" FontWeight="SemiBold" Text="已保存的 ICCID 卡规则" />
                <Border Grid.Column="1" Padding="8,3" Background="{DynamicResource AppInputBrush}" CornerRadius="4">
                  <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding IccidRulesSummaryText, Mode=OneWay}" />
                </Border>
              </Grid>

              <ListView Grid.Row="1" ItemsSource="{Binding IccidRoutes}" SelectedItem="{Binding SelectedIccidRoute, Mode=TwoWay}" BorderThickness="0" Background="Transparent" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True">
                <ListView.ItemContainerStyle>
                  <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                  </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                  <DataTemplate>
                    <Border Padding="12,10" Background="{DynamicResource AppInputBrush}" BorderBrush="{DynamicResource AppCardBorderBrush}" BorderThickness="1" CornerRadius="8">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                          <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="{Binding DisplayName, Mode=OneWay}" />
                            <Border Margin="8,0,0,0" Padding="6,1" Background="{DynamicResource AppCardBrush}" CornerRadius="4">
                              <TextBlock FontSize="10" FontFamily="{DynamicResource AppFontFamily}" Foreground="{DynamicResource AppAccentBrush}" Text="{Binding Iccid, Mode=OneWay}" />
                            </Border>
                          </StackPanel>
                          <TextBlock Margin="0,3,0,0" FontSize="10" Foreground="{DynamicResource AppTextTertiaryBrush}" Text="{Binding ImsiDisplay, Mode=OneWay}" />
                          <TextBlock Margin="0,2,0,0" FontSize="10" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding PhoneDisplay, Mode=OneWay}" />
                        </StackPanel>
                        <Border Grid.Column="1" Margin="12,0" Padding="10,4" Background="{DynamicResource AppCardBrush}" CornerRadius="6" VerticalAlignment="Center">
                          <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding ProxyNodeName, Mode=OneWay}" />
                        </Border>
                        <ui:Button Grid.Column="2" Appearance="Transparent" Padding="6,5" Foreground="{DynamicResource AppDangerBrush}" Command="{Binding DataContext.DeleteIccidRuleCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Icon="{ui:SymbolIcon Symbol=Delete24}" ToolTip="删除后改为跟随 IMSI MCC/PLMN" />
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ListView.ItemTemplate>
              </ListView>
            </Grid>
          </Border>
        </Grid>
      </TabItem>
      
      <!-- TAB 2: IMSI MCC/PLMN fallback rules -->
      <TabItem Header="PLMN 国家规则（次级）">
        <Grid Margin="0,12,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>

          <!-- Top: Quick Add Bar (Search + Dropdown Combo) -->
          <Border
            Grid.Row="0"
            Margin="0,0,0,12"
            Background="{DynamicResource AppCardBrush}"
            BorderBrush="{DynamicResource AppCardBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16,14"
          >
            <Border.Effect>
              <DropShadowEffect BlurRadius="10" Direction="270" Opacity="0.04" ShadowDepth="2" Color="{DynamicResource AppShadowColor}" />
            </Border.Effect>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <!-- Header line: Title + Hint -->
              <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                  <ui:SymbolIcon Symbol="GlobeSearch24" Foreground="{DynamicResource AppAccentBrush}" FontSize="18" Margin="0,0,8,0" />
                  <TextBlock Text="添加国家 / 地区出站分流规则" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Grid.Column="1" FontSize="11" Foreground="{DynamicResource AppTextTertiaryBrush}" Text="自动关联电信 MCC 编码 · 即刻生效" VerticalAlignment="Center" />
              </Grid>

              <!-- Search & Select Row -->
              <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="3*" MinWidth="260" />
                  <ColumnDefinition Width="2*" MinWidth="200" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Combo 1: Search + Select Country Dropdown -->
                <Grid Grid.Column="0" Margin="0,0,10,0">
                  <ComboBox
                    ItemsSource="{Binding FilteredAvailableCountries}"
                    SelectedItem="{Binding SelectedCountryToAdd, Mode=TwoWay}"
                    Text="{Binding CountrySearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    IsEditable="True"
                    IsTextSearchEnabled="False"
                    HorizontalAlignment="Stretch"
                    Padding="10,7"
                  >
                    <ComboBox.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,2">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Grid.Column="0" Text="{Binding Flag}" FontSize="18" VerticalAlignment="Center" />
                          <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource AppTextPrimaryBrush}" VerticalAlignment="Center" Margin="4,0,8,0" />
                          <Border Grid.Column="2" Padding="6,1" Background="{DynamicResource AppInputBrush}" CornerRadius="4" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Code}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AppAccentBrush}" />
                          </Border>
                          <TextBlock Grid.Column="3" Text="{Binding MccSummary}" Foreground="{DynamicResource AppTextTertiaryBrush}" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center" />
                        </Grid>
                      </DataTemplate>
                    </ComboBox.ItemTemplate>
                  </ComboBox>
                </Grid>

                <!-- Combo 2: Egress Route Dropdown -->
                <Grid Grid.Column="1" Margin="0,0,10,0">
                  <ComboBox
                    ItemsSource="{Binding AvailableEgressOptions}"
                    SelectedItem="{Binding QuickAddSelectedEgress, Mode=TwoWay}"
                    DisplayMemberPath="DisplayName"
                    HorizontalAlignment="Stretch"
                    Padding="10,7"
                  />
                </Grid>

                <!-- Button: Add Rule -->
                <ui:Button
                  Grid.Column="2"
                  Appearance="Primary"
                  Padding="18,8"
                  Command="{Binding QuickAddCountryRuleCommand}"
                  Icon="{ui:SymbolIcon Symbol=Add24}"
                  Content="添加规则"
                />
              </Grid>

              <!-- Hint line -->
              <TextBlock
                Grid.Row="2"
                Margin="2,8,0,0"
                FontSize="11"
                Foreground="{DynamicResource AppAccentBrush}"
                Text="{Binding QuickAddHintText, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"
              />
            </Grid>
          </Border>

          <!-- Middle: Rules List Card -->
          <Border
            Grid.Row="1"
            Background="{DynamicResource AppCardBrush}"
            BorderBrush="{DynamicResource AppCardBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16"
          >
            <Border.Effect>
              <DropShadowEffect BlurRadius="10" Direction="270" Opacity="0.04" ShadowDepth="2" Color="{DynamicResource AppShadowColor}" />
            </Border.Effect>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <!-- Header: Table Title + Search Existing Rules + Summary Pill -->
              <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="240" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource AppTextPrimaryBrush}" VerticalAlignment="Center" Text="国家 / 地区分流策略映射表" />

                <Border
                  Grid.Column="1"
                  Margin="10,0,0,0"
                  Background="{DynamicResource ControlFillColorDefaultBrush}"
                  BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  Padding="8,3"
                  CornerRadius="4"
                  VerticalAlignment="Center"
                >
                  <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding RulesSummaryText}" />
                </Border>

                <!-- Filter Search Box for existing rules -->
                <ui:TextBox
                  Grid.Column="3"
                  PlaceholderText="搜索已有规则 (国家/节点/代码)..."
                  Text="{Binding RulesSearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  ClearButtonEnabled="True"
                  Padding="8,4"
                  FontSize="11"
                />
              </Grid>

              <!-- Country Rules ListView -->
              <ListView
                Grid.Row="1"
                ItemsSource="{Binding FilteredCountryRoutes}"
                SelectedItem="{Binding SelectedCountryRoute}"
                BorderThickness="0"
                Background="Transparent"
                Padding="2,2,2,4"
                ScrollViewer.CanContentScroll="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                MouseDoubleClick="OnRuleItemMouseDoubleClick"
              >
                <ListView.ItemContainerStyle>
                  <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                          <Border
                            x:Name="ItemBorder"
                            Background="{DynamicResource AppInputBrush}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource AppCardBorderBrush}"
                            CornerRadius="8"
                            Padding="12,10"
                          >
                            <Border.Effect>
                              <DropShadowEffect BlurRadius="4" Direction="270" Opacity="0.03" ShadowDepth="1" Color="{DynamicResource AppShadowColor}" />
                            </Border.Effect>
                            <ContentPresenter />
                          </Border>
                          <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                              <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                              <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                              <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                              <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
                            </Trigger>
                          </ControlTemplate.Triggers>
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                  </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                  <DataTemplate>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>

                      <!-- Flag Emoji -->
                      <TextBlock Grid.Column="0" FontSize="26" VerticalAlignment="Center" Margin="0,0,14,0" Text="{Binding FlagEmoji, Mode=OneWay}" />

                      <!-- Country Info & MCC -->
                      <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="{Binding CountryName, Mode=OneWay}" />
                          <Border Margin="8,0,0,0" Padding="6,1" Background="{DynamicResource AppInputBrush}" CornerRadius="4" VerticalAlignment="Center">
                            <TextBlock Foreground="{DynamicResource AppAccentBrush}" FontWeight="Bold" FontSize="10" Text="{Binding CountryCode, Mode=OneWay}" />
                          </Border>
                        </StackPanel>
                        <TextBlock Foreground="{DynamicResource AppTextTertiaryBrush}" FontSize="11" Margin="0,3,0,0" Text="{Binding MccDisplay, Mode=OneWay}" />
                      </StackPanel>

                      <!-- Egress Result Badge (Neutral for DIRECT, Emerald for Proxy) -->
                      <Border
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        Padding="12,5"
                        Margin="0,0,12,0"
                        CornerRadius="6"
                        BorderThickness="1"
                      >
                        <Border.Style>
                          <Style TargetType="Border">
                            <Setter Property="Background" Value="{DynamicResource AppSuccessSoftBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AppSuccessBrush}" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsDirect}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Border.Style>
                        <TextBlock FontSize="12" FontWeight="Medium">
                          <TextBlock.Style>
                            <Style TargetType="TextBlock">
                              <Setter Property="Foreground" Value="{DynamicResource AppSuccessBrush}" />
                              <Setter Property="Text" Value="{Binding ProxyNodeName, Mode=OneWay, StringFormat='代理 · {0}'}" />
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirect}" Value="True">
                                  <Setter Property="Foreground" Value="{DynamicResource AppTextSecondaryBrush}" />
                                  <Setter Property="Text" Value="直连 (DIRECT)" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </TextBlock.Style>
                        </TextBlock>
                      </Border>

                      <!-- Row Action Buttons: Edit and Delete -->
                      <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Edit Button -->
                        <ui:Button
                          Appearance="Secondary"
                          Padding="10,5"
                          Margin="0,0,6,0"
                          Command="{Binding DataContext.OpenEditDialogCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                          CommandParameter="{Binding}"
                          Icon="{ui:SymbolIcon Symbol=Edit24}"
                          Content="编辑"
                          ToolTip="编辑此分流规则 (或双击此行)"
                        />

                        <!-- Delete Button -->
                        <ui:Button
                          Appearance="Transparent"
                          Padding="6,5"
                          Foreground="{DynamicResource AppDangerBrush}"
                          Command="{Binding DataContext.DeleteCountryRuleCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                          CommandParameter="{Binding}"
                          Icon="{ui:SymbolIcon Symbol=Delete24}"
                          ToolTip="删除此分流规则"
                        />
                      </StackPanel>
                    </Grid>
                  </DataTemplate>
                </ListView.ItemTemplate>
              </ListView>
            </Grid>
          </Border>
        </Grid>
      </TabItem>

      <!-- TAB 2: 节点预设池 (Node Library) -->
      <TabItem Header="代理节点池">
        <Grid Margin="0,12,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="380" />
          </Grid.ColumnDefinitions>

          <!-- Left: Presets List -->
          <Border
            Grid.Column="0"
            Background="{DynamicResource AppCardBrush}"
            BorderBrush="{DynamicResource AppCardBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="14"
          >
            <Border.Effect>
              <DropShadowEffect BlurRadius="10" Direction="270" Opacity="0.04" ShadowDepth="2" Color="{DynamicResource AppShadowColor}" />
            </Border.Effect>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource AppTextPrimaryBrush}" VerticalAlignment="Center" Text="已登记的 SOCKS5 代理节点" />
                <Border Grid.Column="1" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" Padding="8,3" CornerRadius="4" VerticalAlignment="Center">
                  <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding NodePoolSummaryText}" />
                </Border>
              </Grid>

              <ListView
                Grid.Row="1"
                ItemsSource="{Binding Presets}"
                SelectedItem="{Binding SelectedPreset}"
                BorderThickness="0"
                Background="Transparent"
                Padding="2,2,2,4"
                ScrollViewer.CanContentScroll="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
              >
                <ListView.ItemContainerStyle>
                  <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                          <Border
                            x:Name="NodeBorder"
                            Background="{DynamicResource AppInputBrush}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource AppCardBorderBrush}"
                            CornerRadius="8"
                            Padding="12,10"
                          >
                            <Border.Effect>
                              <DropShadowEffect BlurRadius="4" Direction="270" Opacity="0.03" ShadowDepth="1" Color="{DynamicResource AppShadowColor}" />
                            </Border.Effect>
                            <ContentPresenter />
                          </Border>
                          <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                              <Setter TargetName="NodeBorder" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                              <Setter TargetName="NodeBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                              <Setter TargetName="NodeBorder" Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                              <Setter TargetName="NodeBorder" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
                            </Trigger>
                          </ControlTemplate.Triggers>
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                  </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                  <DataTemplate>
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>

                      <!-- Node Name & Address -->
                      <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="{Binding Name, Mode=OneWay}" />
                        <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,2,0,0" Text="{Binding Url, Mode=OneWay}" TextTrimming="CharacterEllipsis" MaxWidth="280" />
                      </StackPanel>

                      <!-- Latency Badge -->
                      <Border
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Padding="8,3"
                        Margin="0,0,10,0"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        CornerRadius="4"
                      >
                        <TextBlock FontSize="11" FontWeight="Medium" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding LatencyDisplay, Mode=OneWay}" />
                      </Border>

                      <!-- Inline Quick Test Button -->
                      <ui:Button
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        Appearance="Secondary"
                        Padding="8,4"
                        Margin="0,0,6,0"
                        Command="{Binding DataContext.TestPresetLatencyCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                        CommandParameter="{Binding}"
                        Content="测速"
                        FontSize="11"
                      />

                      <!-- Delete Node Button -->
                      <ui:Button
                        Grid.Column="3"
                        VerticalAlignment="Center"
                        Appearance="Transparent"
                        Command="{Binding DataContext.DeletePresetCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                        CommandParameter="{Binding}"
                        Icon="{ui:SymbolIcon Symbol=Delete24}"
                        ToolTip="删除此代理节点"
                      />
                    </Grid>
                  </DataTemplate>
                </ListView.ItemTemplate>
              </ListView>
            </Grid>
          </Border>

          <!-- Right: Add Preset Card & Slot Fallback Card -->
          <StackPanel Grid.Column="2">
            <!-- Add Preset Card -->
            <Border
              Background="{DynamicResource AppCardBrush}"
              BorderBrush="{DynamicResource AppCardBorderBrush}"
              BorderThickness="1"
              CornerRadius="12"
              Padding="16"
              Margin="0,0,0,14"
            >
              <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" Opacity="0.04" ShadowDepth="2" Color="{DynamicResource AppShadowColor}" />
              </Border.Effect>
              <StackPanel>
                <TextBlock FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="➕ 添加代理节点" Margin="0,0,0,8" />

                <!-- URL Quick Add -->
                <TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="方式 1: 通过 URL 快速导入" Margin="0,0,0,4" />
                <Grid Margin="0,0,0,14">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ui:TextBox
                    Grid.Column="0"
                    Margin="0,0,8,0"
                    PlaceholderText="socks5://user:pass@host:port"
                    Text="{Binding NewPresetUrl, UpdateSourceTrigger=PropertyChanged}"
                  />
                  <ui:Button
                    Grid.Column="1"
                    Appearance="Primary"
                    Command="{Binding AddPresetCommand}"
                    Content="导入"
                  />
                </Grid>

                <Separator Margin="0,0,0,12" />

                <!-- Manual Add Form -->
                <TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="方式 2: 手动配置节点参数" Margin="0,0,0,6" />
                
                <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="节点名称：" Margin="0,0,0,2" />
                <ui:TextBox Margin="0,0,0,8" Text="{Binding NewPresetName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="例如: 专用香港节点" />

                <Grid Margin="0,0,0,8">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="80" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0">
                    <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="服务器 IP / 域名：" Margin="0,0,0,2" />
                    <ui:TextBox Text="{Binding NewPresetHost, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="127.0.0.1" />
                  </StackPanel>
                  <StackPanel Grid.Column="2">
                    <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="端口：" Margin="0,0,0,2" />
                    <ui:TextBox Text="{Binding NewPresetPort, UpdateSourceTrigger=PropertyChanged}" />
                  </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0">
                    <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="用户名 (可选)：" Margin="0,0,0,2" />
                    <ui:TextBox Text="{Binding NewPresetUsername, UpdateSourceTrigger=PropertyChanged}" />
                  </StackPanel>
                  <StackPanel Grid.Column="2">
                    <TextBlock FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="密码 (可选)：" Margin="0,0,0,2" />
                    <ui:PasswordBox Password="{Binding NewPresetPassword, UpdateSourceTrigger=PropertyChanged}" />
                  </StackPanel>
                </Grid>

                <ui:Button
                  Appearance="Primary"
                  Command="{Binding AddNewPresetCommand}"
                  Content="保存至节点池"
                  HorizontalAlignment="Stretch"
                />
              </StackPanel>
            </Border>

            <!-- Routing priority explanation -->
            <Border
              Background="{DynamicResource AppCardBrush}"
              BorderBrush="{DynamicResource AppCardBorderBrush}"
              BorderThickness="1"
              CornerRadius="12"
              Padding="16"
            >
              <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" Opacity="0.04" ShadowDepth="2" Color="{DynamicResource AppShadowColor}" />
              </Border.Effect>
              <StackPanel>
                <TextBlock FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="分流优先级" Margin="0,0,0,8" />
                <TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="1. ICCID 卡规则（最高）" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="2. IMSI MCC/PLMN 国家规则" Margin="0,0,0,4" />
                <TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="3. 未命中时直连 UDP" Margin="0,0,0,8" />
                <TextBlock FontSize="11" Foreground="{DynamicResource AppTextTertiaryBrush}" Text="硬件卡槽不再保存独立代理，换卡后不会继承上一张卡的出口。" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>
      </TabItem>

    </TabControl>

    <!-- Modal Edit Dialog Overlay (悬浮窗编辑 / Dialog) -->
    <Grid
      x:Name="EditDialogOverlay"
      Grid.RowSpan="3"
      Margin="-24,-18,-24,-24"
      Background="{DynamicResource AppOverlayBrush}"
      Visibility="{Binding IsEditingRoute, Converter={StaticResource BoolToVis}}"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Panel.ZIndex="200"
    >
      <!-- Centered Dialog Card -->
      <Border
        Width="500"
        MaxHeight="600"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
        Background="{DynamicResource AppCardBrush}"
        BorderBrush="{DynamicResource AppCardBorderBrush}"
        BorderThickness="1"
        CornerRadius="16"
        Padding="24,20"
      >
        <Border.Effect>
          <DropShadowEffect BlurRadius="32" Direction="270" Opacity="0.30" ShadowDepth="8" Color="{DynamicResource AppShadowColor}" />
        </Border.Effect>

        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <!-- Header with Title & Close button -->
          <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
              <ui:SymbolIcon Symbol="Edit24" Foreground="{DynamicResource AppAccentBrush}" FontSize="18" Margin="0,0,8,0" />
              <TextBlock Text="编辑国家分流策略" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" VerticalAlignment="Center" />
            </StackPanel>
            <ui:Button
              Grid.Column="1"
              Appearance="Transparent"
              Padding="6,4"
              Icon="{ui:SymbolIcon Symbol=Dismiss24}"
              Command="{Binding CloseEditDialogCommand}"
              ToolTip="关闭 (Esc)"
            />
          </Grid>

          <!-- Target Country Info Card -->
          <Border
            Grid.Row="1"
            Padding="14,12"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource AppCardBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Margin="0,0,0,16"
          >
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{Binding EditingRoute.FlagEmoji, FallbackValue='🌐'}" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0" />
              <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding EditingRoute.CountryName, FallbackValue='--'}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" />
                  <Border Margin="8,0,0,0" Padding="6,1" Background="{DynamicResource AppInputBrush}" CornerRadius="4">
                    <TextBlock Text="{Binding EditingRoute.CountryCode, FallbackValue='--'}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AppAccentBrush}" />
                  </Border>
                </StackPanel>
                <TextBlock Text="{Binding EditingRoute.MccDisplay, FallbackValue='--', Mode=OneWay}" FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,4,0,0" />
              </StackPanel>
            </Grid>
          </Border>

          <!-- Egress Mode Radio Selection -->
          <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="出站分流策略：" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Margin="0,0,0,10" />

            <!-- Option 1: Direct -->
            <Border
              Padding="12,10"
              Margin="0,0,0,8"
              Background="{DynamicResource AppInputBrush}"
              BorderBrush="{DynamicResource AppCardBorderBrush}"
              BorderThickness="1"
              CornerRadius="8"
            >
              <RadioButton
                GroupName="EditDialogEgressGroup"
                IsChecked="{Binding EditDialogIsDirect, Mode=TwoWay}"
              >
                <StackPanel Margin="6,0,0,0">
                  <TextBlock Text="直连模式 (DIRECT)" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource AppTextPrimaryBrush}" />
                  <TextBlock Text="通过默认物理网卡直连运营商核心网，不走代理隧道" FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,2,0,0" />
                </StackPanel>
              </RadioButton>
            </Border>

            <!-- Option 2: Proxy -->
            <Border
              Padding="12,10"
              Background="{DynamicResource AppInputBrush}"
              BorderBrush="{DynamicResource AppCardBorderBrush}"
              BorderThickness="1"
              CornerRadius="8"
            >
              <RadioButton
                GroupName="EditDialogEgressGroup"
                IsChecked="{Binding EditDialogIsProxy, Mode=TwoWay}"
              >
                <StackPanel Margin="6,0,0,0">
                  <TextBlock Text="代理节点 (Proxy)" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource AppTextPrimaryBrush}" />
                  <TextBlock Text="经由指定 SOCKS5 代理隧道转发 UDP 流量" FontSize="11" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,2,0,0" />
                </StackPanel>
              </RadioButton>
            </Border>
          </StackPanel>

          <!-- Node Selector (Enabled when Proxy is selected) -->
          <StackPanel Grid.Row="3" Margin="0,0,0,18" IsEnabled="{Binding EditDialogIsProxy}">
            <TextBlock Text="选择出站代理节点：" FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Margin="0,0,0,6" />
            <ComboBox
              ItemsSource="{Binding Presets}"
              SelectedItem="{Binding EditDialogSelectedNode, Mode=TwoWay}"
              DisplayMemberPath="Name"
              HorizontalAlignment="Stretch"
              Padding="10,7"
            />
          </StackPanel>

          <!-- Separator -->
          <Separator Grid.Row="4" Margin="0,0,0,16" Background="{DynamicResource AppCardBorderBrush}" />

          <!-- Action Footer -->
          <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Delete Button -->
            <ui:Button
              Grid.Column="0"
              Appearance="Transparent"
              Foreground="{DynamicResource AppDangerBrush}"
              Icon="{ui:SymbolIcon Symbol=Delete24}"
              Command="{Binding DeleteCountryRuleCommand}"
              CommandParameter="{Binding EditingRoute}"
              Content="删除此规则"
              FontSize="12"
            />

            <!-- Cancel Button -->
            <ui:Button
              Grid.Column="2"
              Appearance="Secondary"
              Content="取消"
              Command="{Binding CloseEditDialogCommand}"
              Margin="0,0,8,0"
              Padding="18,7"
            />

            <!-- Save Changes Button -->
            <ui:Button
              Grid.Column="3"
              Appearance="Primary"
              Content="保存更改"
              Command="{Binding SaveEditDialogCommand}"
              Padding="20,7"
            />
          </Grid>
        </Grid>
      </Border>
    </Grid>
  </Grid>
</Page>
